# Letter scramble button

Sharp rectangle (`border-radius: 0`). Hover runs a left-to-right letter decode. Glyph pool is `A–Z` and `a–z` only — no digits, no icons, no symbols.

Prefix: `btn-lscram-`. Grayscale. Styles tray after `stroke-move-2`.
